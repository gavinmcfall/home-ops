---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./externalsecret.yaml
  - ./pvc.yaml
  - ./helmrelease.yaml
components:
  - ../../../../components/keda/nfs-scaler
  - ../../../../components/gatus/external
  - ../../../../components/volsync-standard
  - ../../../../components/priority/priority-03
patches:
  - target:
      group: keda.sh
      version: v1alpha1
      kind: ScaledObject
      name: plex
    patch: |
      - op: replace
        path: /spec/minReplicaCount
        value: 1
configMapGenerator:
  - name: plex-loki-rules
    files:
      - plex.yaml=./resources/lokirule.yaml
    options:
      labels:
        loki_rule: "true"
      annotations:
        # Loki's rules sidecar files this under the single-tenant ("fake") rule directory.
        k8s-sidecar-target-directory: /rules/fake
generatorOptions:
  disableNameSuffixHash: true
