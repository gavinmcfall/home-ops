---
apiVersion: v1
kind: Service
metadata:
  name: mariadb-exporter
  labels:
    app.kubernetes.io/name: mariadb-exporter
    app.kubernetes.io/component: metrics
spec:
  selector:
    app.kubernetes.io/name: mariadb-exporter
    app.kubernetes.io/component: metrics
  ports:
    - name: metrics
      port: 9104
      targetPort: metrics
