---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: pgbackrest-controller
  name: pgbackrest-controller
  namespace: database
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pgbackrest-controller
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: pgbackrest-controller
    spec:
      containers:
        - name: pgbackrest-controller
          image: registry.hub.docker.com/dalibo/cnpg-pgbackrest-controller:0.0.3@sha256:9563249502db7c1ae22c5e1979ab7ffd040559ea8c7544bfad25c5254c7ceb2e
          imagePullPolicy: IfNotPresent
          args:
            - operator
            - --server-cert=/server/tls.crt
            - --server-key=/server/tls.key
            - --client-cert=/client/tls.crt
            - --server-address=:9090
            - --log-level=debug
          env:
            - name: SIDECAR_IMAGE
              value: registry.hub.docker.com/dalibo/cnpg-pgbackrest-sidecar:0.0.2@sha256:b4feb69660a67320590358f435fda7bf5733b7151bca297aef970c2edf54e008
            # The plugin injects a plugin-pgbackrest-exporter init container into every
            # instance pod even when PluginConfig.exporterConfig.enabled: false.
            # Without SIDECAR_EXPORTER_IMAGE, the init container defaults to a bare
            # "pgbackrest-sidecar-exporter" ref that resolves to docker.io/library/...
            # which doesn't exist, blocking the standby rollout with ImagePullBackOff.
            - name: SIDECAR_EXPORTER_IMAGE
              value: registry.hub.docker.com/dalibo/cnpg-pgbackrest-sidecar-exporter:0.0.2@sha256:92b26f75fccd2b37fcc6b45505c10ed1e2ddb32aca5ca3b68eec8b1b893cd7a9
          ports:
            - containerPort: 9090
              protocol: TCP
          readinessProbe:
            initialDelaySeconds: 10
            periodSeconds: 10
            tcpSocket:
              port: 9090
          resources:
            requests:
              cpu: 10m
              memory: 64Mi
            limits:
              memory: 256Mi
          volumeMounts:
            - mountPath: /server
              name: server
            - mountPath: /client
              name: client
      serviceAccountName: pgbackrest-controller
      # Allow scheduling on pyro-01 (GPU worker) as a fallback when all 3
      # stantons are under pressure or one is cordoned for maintenance.
      # Without this, a stanton drain can leave pgbackrest-controller
      # Pending -> CNPG plugin gRPC dies -> cluster reconciliation halts
      # -> postgres18-cluster instance reschedule blocked -> drain stuck.
      # See incident 2026-05-18 (cluster-capacity-blockers memory).
      tolerations:
        - key: nvidia.com/gpu
          operator: Equal
          value: "true"
          effect: NoExecute
      volumes:
        - name: server
          secret:
            secretName: pgbackrest-controller-server-tls
        - name: client
          secret:
            secretName: pgbackrest-controller-client-tls
