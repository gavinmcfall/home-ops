---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./externalsecret.yaml
  # NOTE: externalsecret-flux.yaml moved to pgbackrest/ folder
  # It must be deployed before this Kustomization for Flux substitution to work
  - ./repository.yaml
  - ./cluster.yaml
  - ./pooler.yaml
  - ./scheduledbackup.yaml
  - ./service.yaml
