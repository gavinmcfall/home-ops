---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/common-4.4.0/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app influxdb3
spec:
  interval: 30m
  chart:
    spec:
      chart: app-template
      version: 4.4.0
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      strategy: rollback
      retries: 3
  dependsOn:
    - name: rook-ceph-cluster
      namespace: rook-ceph
  values:
    controllers:
      influxdb3:
        type: statefulset
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          app:
            image:
              repository: docker.io/library/influxdb
              tag: 3-enterprise@sha256:835229f278ea2dd7e139450d90ffa5af00dc731cb3dd9379ba42faa250b8a697
            # DEPLOY-VERIFY: confirm the exact `serve` flags against the running
            # image (`kubectl exec ... -- influxdb3 serve --help`) before first boot.
            # In particular confirm whether Enterprise single-node requires
            # `--cluster-id`, and the health endpoint path.
            args:
              - serve
              - --node-id=influxdb3-node0
              - --cluster-id=influxdb3
              - --object-store=s3
              - --bucket=influxdb-telemetry
              - --data-dir=/var/lib/influxdb3
              - --http-bind=0.0.0.0:8181
              - --aws-endpoint=http://rook-ceph-rgw-ceph-objectstore.rook-ceph.svc:80
              - --aws-default-region=us-east-1
              - --aws-allow-http
            env:
              AWS_ALLOW_HTTP: "true"
            envFrom:
              # OBC-generated creds: AWS_ACCESS_KEY_ID / AWS_SECRET_ACCESS_KEY
              - secretRef:
                  name: influxdb3
              # License email (+ admin token, unused by serve)
              - secretRef:
                  name: influxdb3-secret
            probes:
              liveness: &probes
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /health
                    port: &port 8181
                  initialDelaySeconds: 30
                  periodSeconds: 10
                  failureThreshold: 6
              readiness: *probes
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: false
              capabilities: { drop: ["ALL"] }
            resources:
              requests:
                cpu: 100m
                memory: 512Mi
              limits:
                memory: 4Gi
    defaultPodOptions:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1500
        runAsGroup: 1500
        fsGroup: 1500
        fsGroupChangePolicy: OnRootMismatch
        seccompProfile: { type: RuntimeDefault }
    service:
      app:
        controller: influxdb3
        ports:
          http:
            port: *port
    persistence:
      data:
        existingClaim: influxdb3
        globalMounts:
          - path: /var/lib/influxdb3
