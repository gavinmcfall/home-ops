---
apiVersion: v1
kind: Service
metadata:
  name: truenas
  annotations:
    external-dns.alpha.kubernetes.io/hostname: citadel.nerdz.cloud
    internal-dns.alpha.kubernetes.io/target: internal.nerdz.cloud
spec:
  type: ClusterIP
  ports:
    - name: https
      protocol: TCP
      port: 443
      targetPort: 443
      appProtocol: https
