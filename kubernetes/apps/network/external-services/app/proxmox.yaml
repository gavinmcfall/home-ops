---
apiVersion: v1
kind: Service
metadata:
  name: proxmox
  namespace: network
  labels:
    # Add this label for EndpointSlice discovery
    service.kubernetes.io/headless: "false"
spec:
  # Remove the selector to make it a headless service
  clusterIP: None
  ports:
    - name: https
      protocol: TCP
      port: 8006
      targetPort: 8006
---
apiVersion: discovery.k8s.io/v1
kind: EndpointSlice
metadata:
  name: proxmox-1
  namespace: network
  labels:
    kubernetes.io/service-name: proxmox
    endpointslice.kubernetes.io/managed-by: manual
addressType: IPv4
endpoints:
  - addresses:
      - "${SECRET_PROXMOX_IP}"
    conditions:
      ready: true
      serving: true
      terminating: false
ports:
  - name: https
    port: 8006
    protocol: TCP
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: proxmox
  namespace: network
  labels:
    app.kubernetes.io/name: proxmox
    app.kubernetes.io/instance: proxmox
  annotations:
    external-dns.alpha.kubernetes.io/target: internal.${SECRET_DOMAIN}
spec:
  parentRefs:
    - name: internal
      namespace: network
      sectionName: https
  hostnames:
    - nexus.${SECRET_DOMAIN}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: proxmox
          port: 8006
