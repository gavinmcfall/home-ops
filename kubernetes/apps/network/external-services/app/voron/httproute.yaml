---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: voron
  labels:
    app.kubernetes.io/name: voron
    app.kubernetes.io/instance: voron
  annotations:
    internal-dns.alpha.kubernetes.io/target: internal.nerdz.cloud
spec:
  parentRefs:
    - name: internal
      namespace: network
      sectionName: https
  hostnames:
    - voron.nerdz.cloud
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: voron
          port: 80
