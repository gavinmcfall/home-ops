---
apiVersion: v1
kind: Service
metadata:
  name: unifi
  namespace: network
spec:
  type: ClusterIP
  ports:
    - name: https
      protocol: TCP
      port: 443
      targetPort: 443
---
apiVersion: discovery.k8s.io/v1
kind: EndpointSlice
metadata:
  name: unifi-external
  namespace: network
  labels:
    kubernetes.io/service-name: unifi
    endpointslice.kubernetes.io/managed-by: external-services-controller
addressType: IPv4
endpoints:
  - addresses:
      - "${SECRET_UNIFI_IP}"
    conditions:
      ready: true
      serving: true
      terminating: false
ports:
  - name: https
    port: 443
    protocol: TCP
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: unifi
  namespace: network
  labels:
    app.kubernetes.io/name: unifi
    app.kubernetes.io/instance: unifi
  annotations:
    external-dns.alpha.kubernetes.io/target: internal.${SECRET_DOMAIN}
spec:
  parentRefs:
    - name: internal
      namespace: network
      sectionName: https
  hostnames:
    - unifi.${SECRET_DOMAIN}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: unifi
          port: 443
