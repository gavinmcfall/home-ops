---
apiVersion: v1
kind: Service
metadata:
  name: proxmox
  namespace: networking
spec:
  type: ExternalName
  externalName: nexus.${bootstrap_cloudflare_domain}
  ports:
    - name: http
      port: &port 8006
      protocol: TCP
      targetPort: *port