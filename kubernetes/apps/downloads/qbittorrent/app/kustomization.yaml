---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./externalsecret.yaml
  - ./helmrelease.yaml
components:
  # Restore from old Restic backup in Backblaze B2 - no KEDA, no Kopia
  - ../../../../components/volsync-restic-restore
configMapGenerator:
  - name: qbittorrent-scripts
    files:
      - completed.sh=./resources/completed.sh
generatorOptions:
  disableNameSuffixHash: true
