---
# yaml-language-server: $schema=https://kubernetes-schemas.nerdz.cloud/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: pocket-id
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: pocket-id-secret
    template:
      engineVersion: v2
      data:
        INIT_POSTGRES_DBNAME: pocket_id
        INIT_POSTGRES_HOST: postgres18-cluster-rw.database.svc.cluster.local
        INIT_POSTGRES_USER: "{{ .POCKET_ID_POSTGRES_USER }}"
        INIT_POSTGRES_PASS: "{{ .POCKET_ID_POSTGRES_PASSWORD }}"
        INIT_POSTGRES_SUPER_PASS: "{{ .POSTGRES_SUPER_PASS }}"
        DB_CONNECTION_STRING: "postgres://{{.POCKET_ID_POSTGRES_USER}}:{{.POCKET_ID_POSTGRES_PASSWORD}}@postgres18-cluster-rw.database.svc.cluster.local:5432/pocket_id?sslmode=disable"
        ENCRYPTION_KEY: "{{ .POCKET_ID_ENCRYPTION_KEY }}"
        MAXMIND_LICENSE_KEY: "{{ .POCKET_ID_MAXMIND_LICENSE_KEY }}"
  dataFrom:
    - extract:
        key: pocket-id
    - extract:
        key: cloudnative-pg
