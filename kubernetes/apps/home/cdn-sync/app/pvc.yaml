---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cdn-sync-data
  namespace: home
  labels:
    app.kubernetes.io/name: cdn-sync
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-block
  resources:
    requests:
      storage: 5Gi
