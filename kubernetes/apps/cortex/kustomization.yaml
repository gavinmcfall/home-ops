---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: cortex

components:
  - ../../components/common

resources:
  # Applications
  - ./local-ai/ks.yaml
  # - ./ollama/ks.yaml  # REPLACED by LocalAI
  # - ./openclaw/ks.yaml  # TODO: rewire OLLAMA_URL → LocalAI, then uncomment
