---
# yaml-language-server: $schema=https://kubernetes-schemas.nerdz.cloud/notification.toolkit.fluxcd.io/provider_v1beta3.json
apiVersion: notification.toolkit.fluxcd.io/v1beta3
kind: Provider
metadata:
  name: discord-flux
spec:
  type: discord
  secretRef:
    name: discord-flux-webhook-secret
